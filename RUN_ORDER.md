# Recommended analysis order

1. Run `01_data_preprocessing/`.
2. Run `02_bulk_analysis/`.
3. Run `03_scRNA_discovery/`.
4. Run `04_hdWGCNA/`.
5. Run `05_scRNA_validation/`.
6. Run `06_DRIP_validation/`.
7. Run `07_virtual_knockout/`.
8. Run `08_RNAPII_validation/`.

Within each directory, run scripts in numerical order.
